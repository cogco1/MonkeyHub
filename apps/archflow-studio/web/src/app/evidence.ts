/** The three tabs of the evidence drawer, named as the cards link to them. */
export type EvidenceTab = "honesty" | "receipts" | "events";

/** Where the drawer's pinned state is remembered, per browser, never shared. */
export const EVIDENCE_PINNED_KEY = "archflow-studio.evidence.pinned";
