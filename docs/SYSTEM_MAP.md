# ArchFlow system map

> Generated by `python tools/devctl.py render-map` from `governance/module_registry.json`.
> One owner per capability. Read this before the tree; read `docs/CANONICAL_SPINE.md` for why.

## adapters

### adapters.cad_execution — `archflow/adapters/cad_execution.py`
Prepare, supervise and independently verify one fail-closed Rhino .3dm export of an exact compiled program.
- owns: RhinoCadProgramBinding and RhinoCadExportIdentity: the P036 record/branch/stage identity an export is bound to; the immutable export plan: workspace layout, script, completion token, host witness and the full validation denominator digest; the STA PowerShell/COM worker source and its shell-free encoded command; supervising the blocking worker, collecting its output, and independently cleaning the Rhino host it started; readback verification: the saved file's physical objects, semantics, bounds, counts, layer colours and document user text against the denominator; the execution receipt and its status
- does not own: translating the program into a script - adapters.cad_program; selecting what a patch rebuilds - adapters.cad_patch; reading the .3dm - adapters.three_dm_inspector; writing anything into the project repository - it has no project-repository or canonical-write authority; runtime.project_runner retains the receipt; choosing the workspace - the caller supplies an existing speculative workspace
- api: `prepare_rhino_three_dm_export`, `execute_rhino_three_dm_export`, `verify_rhino_export_readback`, `build_rhino_com_powershell_source`, `build_rhino_com_powershell_command`, `discover_rhino_executables`, `RhinoCadProgramBinding`, `RhinoPatchBase`, `CadExecutionStatus`, `CadExecutionError`
- invariants: a successful process exit is never sufficient - the saved file is read back independently and must match the denominator; the export identity refuses any up_axis but Z-up and any unit outside millimeter/meter/inch/foot; every path is constrained to the caller-supplied workspace; no shell interpolation and no launcher file exists; the plan is immutable once prepared and identified by its plan digest; discovery is read-only: it never launches or attaches to Rhino

### adapters.cad_patch — `archflow/adapters/cad_patch.py`
Diff two compiled programs into the closed set of operations a Rhino rebuild must redo, and the prelude that carries the rest of the prior document forward.
- owns: per-object structural digests of what the CAD script actually emits; the diff into changed / added / retired / kept object ids, with a reason per object; closing the rebuild set over input edges (consumers of a rebuilt object, producers of a rebuilt operation's inputs); the Rhino-side prelude that opens the prior model, deletes the retired objects and re-stamps the kept ones; refusing a patch that cannot be expressed, so the caller rebuilds instead
- does not own: emitting the rebuilt operations - adapters.cad_program's operation_subset does; running or verifying the patched export - adapters.cad_execution; deciding whether to patch at all - runtime.project_runner's _export chooses reuse, patch or rebuild; comparing two runs' saved geometry - studio.validation's compare
- api: `select_patch_operations`, `build_patch_prelude`, `PatchSelection`, `CadPatchError`
- invariants: an object is rebuilt only when its structural digest or its analytic bounds actually changed, beyond tolerance; the rebuild set is closed over input edges - nothing consumed or produced by a rebuilt operation is left stale; an inexpressible patch fails CadPatchError, which is a signal to rebuild, never a partial document

### adapters.cad_program — `archflow/adapters/cad_program.py`
Translate a compiled neutral geometry program into one deterministic, semantics-carrying rhinoscriptsyntax build script, plus the analytic denominator it must reproduce.
- owns: the supported operation vocabulary and its rhinoscriptsyntax emission (solid, revolve, extrusion, loft, boolean union/difference/intersection, linear and radial array, transform, curve); the layer path and layer colour tables derived from components and material assignment; the archflow:* object user text each physical object must carry (expected_object_semantics); the analytic per-object bounds the realization must reproduce (expected_object_bounds); applying a datum-bound base_level to profile points (lift_to_base_level); the P103 partial emission of an operation subset
- does not own: running the script, saving the file or verifying the readback - adapters.cad_execution; deciding which operations a patch rebuilds - adapters.cad_patch; reading the resulting .3dm - adapters.three_dm_inspector; compiling the program - compilers.geometry; any design decision: it holds no design authority and adds no geometry
- api: `translate_to_rhino_python`, `expected_object_semantics`, `expected_object_bounds`, `lift_to_base_level`, `CadTranslation`, `CadTranslationError`
- invariants: an unsupported operation kind fails CadTranslationError rather than being dropped silently; the same program translates to the same script bytes - the emission is deterministic and self-contained; coordinates pass through as the program states them; the translator performs no frame conversion; every physical object is named and carries its archflow:* identity strings

### adapters.three_dm_inspector — `archflow/adapters/three_dm_inspector.py`
Read one .3dm file headlessly and return a deterministic, JSON-compatible summary of what is actually saved in it.
- owns: the ThreeDmInspection shape and its versioned key sets (the one place the schema's required keys are declared); reading layers, objects, instance definitions and references, counts by type and by layer; document and object user strings; world bounds: aggregate, per named object, and the visible-bounds witnesses that back each leaf bound; native material and render-material tables and each object's material binding; per-object encoded-geometry digests and morphology analysis (planarity, curve sampling); the typed, fail-closed inspection error codes
- does not own: producing the file - adapters.cad_execution; deciding whether the file matches a program - adapters.cad_execution's readback verification; importing RhinoCommon, starting Rhino, or choosing where anything is persisted; comparing two inspections - studio.validation's compare
- api: `inspect_three_dm`, `ThreeDmInspection`, `ThreeDmInspectionError`, `ThreeDmInspectionErrorCode`, `REQUIRED_INSPECTION_KEYS`, `SUPPORTED_INSPECTION_SCHEMAS`
- invariants: read-only: rhino3dm is imported lazily, one immutable byte snapshot is read, and no Rhino process is started; bounds come from saved visible geometry, never from Brep control points; an object geometry digest hashes encoded geometry only - object attributes cannot change it; an unreadable or unsupported file fails with a named error code, not a partial summary

## capabilities

### capabilities.declaration — `archflow/capabilities/declaration.py`
Define and gate the mandatory typed decisions a stage requires, and compile the gate-passed ones into hard commitments.
- owns: DeclarationQuadrant and DeclarationKind: the vocabulary a stage's decision fields are grouped and typed by; DeclarationField and StageDeclarationContract, with each field's unit, authorized range and range provenance; the deterministic geometry cross-checks that bind a declared value to the accepted massing; the fail-closed stage gate over a declaration set (validate_stage_declarations); compiling gate-passed declarations of named quadrants into HARD Commitments; selecting exactly the decision basis shards a contract's fields need
- does not own: storing declared field values - the framework holds contracts, the provider supplies values; the commitment lifecycle and validation - archflow.state.commitments and archflow.validation; which seat may declare in which quadrant - capabilities.discipline_seats; stage opening or closure - archflow.state.stage_workflow and control.stage_closure
- api: `DeclarationQuadrant`, `DeclarationKind`, `DeclarationField`, `DeclarationError`, `GeometryCheck`, `StageDeclarationContract`, `validate_stage_declarations`, `compile_declaration_commitments`, `select_decision_basis`
- invariants: the gate fails closed: a missing, out-of-range or geometry-contradicted field blocks the stage; a declared value is checked against the massing by a deterministic measure, within the contract's tolerance ratio; only gate-passed declarations become commitments, and only in the quadrants named

### capabilities.discipline_seats — `archflow/capabilities/discipline_seats.py`
Divide authoring across role-scoped seats: what a seat may write, what it may read, what it hands the next seat, and in which round it runs.
- owns: SeatSpec: the component subtrees a seat writes, its disciplines, phases, quadrants, the seats it consumes and whether it reviews; the owned subtree and ancestor computation over the component tree; compiling a deterministic SeatHandover from a committed seat: its datums, exclusion bounds, digests and object constraints; projecting the design state onto one seat as SeatAuthoringContext, so sibling subtrees never leak; refusing a seat that publishes a datum over a project level or grid axis; checking declared engagements against a handover's exclusion bounds; scheduling seats into parallel rounds by handover dependency
- does not own: producing any geometry inside a seat's scope - capabilities.element_producers; running the rounds, exporting or writing seat records - runtime.project_runner; the declaration contract and its gate - capabilities.declaration; who is authorized to write canonically - archflow.contracts.authority (seats carry no_authority); reading the project's authored seat pack from disk - studio.candidate's seats adapter and tools/run_project.py
- api: `SeatSpec`, `SeatError`, `SeatHandover`, `HandoverKind`, `DeclaredEngagement`, `owned_subtree`, `ancestors`, `compile_handover`, `project_seat_context`, `check_seat_datums`, `check_handover_exclusions`, `schedule_seats`
- invariants: a seat's authoring context contains only its subtree, its ancestors and the handovers it consumes - a sibling subtree is never visible; a handover is bound to the base state digest it was compiled from and carries its own digest; a seat may not publish a datum id that a project level or grid axis already owns; entering another seat's exclusion bound is a violation unless a DeclaredEngagement is on record; seats carry explicit no_authority for canonical write, design and stage acceptance

### capabilities.element_producers — `archflow/capabilities/element_producers.py`
Turn a State Record's Element@1 rows into geometry operations, datum bindings, published datums and materialized relations, reading references rather than computing coordinates.
- owns: the producer dispatch table PRODUCERS: column-array, capitals, beam, pediment, wall, prism, ring, loft, dome-cap, declined; reading Element@1 entities off a StateRecord into ElementRow, in production order; topological production order: an element that names another (or its published `<id>-top`) is produced after it; publishing `<id>-top` interface datums an element grants to what it supports; declaring the support relations that construction materializes (ProducedRelation), with engagement or rise; typed declination: a component owned, looked at, and left without geometry for a stated reason; the building-local frame id and the +Y extrusion convention every producer emits
- does not own: turning a reference into a plan coordinate or an elevation datum - capabilities.reference_resolver; the wall solid, its through-cuts and its hosted voids - capabilities.wall_solver; window and door member geometry inside a void - capabilities.opening_solver; resolving a symbolic datum binding into a number - compilers.geometry; measuring whether a declared relation actually holds - capabilities.relation_checks; which rows a seat may produce, and the seat's exclusions - runtime.project_runner and capabilities.discipline_seats
- api: `ElementProducerError`, `ElementRow`, `ProductionContext`, `element_rows_of`, `produce_rows`
- invariants: no producer computes a plan coordinate itself; every position goes through resolve_plan; elevations leave as (datum id, offset) bindings, never as literal numbers; an element referencing another is produced after it; a reference cycle or a missing element fails ElementProducerError; an unknown producer name fails typed rather than being skipped; an overlap into a support exists only as a declared ENGAGEMENT parameter, recorded on the relation

### capabilities.element_reindex (None) — `archflow/capabilities/element_reindex.py`
Recover Element@1 identity from an exported model without moving it: objects (3dm inspection) -> component/family/side -> element drafts with symbolic references where the geometry snaps to declared axes and levels, proven by re-producing the drafted rows and measuring the residual; ambiguity and source conflicts are named, never resolved by guessing.
- owns: reading SourceObjects off a ThreeDmInspectionSummary (named bboxes, archflow:component / archflow:producer_op strings, geometry digests); family and side of an object from its producer op (column-west-3 -> column / west); precedence between sources per (component, side): a patch supersedes the base, equal patches are ALTERNATE and the base stands; element drafts per (component, family, side): column-array, capitals, beam, prism from boxes; AMBIGUOUS for forms boxes cannot carry; drafting a GridAxis@1 the model needs and the record lacks (a column line), marked derived; the residual between produced and inspected boxes, by running the real producers on the drafted rows; derived support_contact relations from vertical contact within the stage-5 tolerance; the ComponentCatalog@1 payload and the successor record that adds axes, rows and relations and replaces nothing
- does not own: producing geometry from a row - capabilities.element_producers; resolving a reference - capabilities.reference_resolver; writing any record - tools/reindex_project.py through project.repository; the studio's derived catalog of an authored record - studio.binding
- api: `reindex`, `ReindexResult`, `ElementDraft`, `SourceObject`, `objects_of`, `place_objects`, `family_of`, `side_of`, `Frame`

### capabilities.geometry_proposal — `archflow/capabilities/geometry_proposal.py`
Author, compile and retain bounded rounds of a provider's neutral geometry proposal against one exact state, with no fallback and no acceptance authority.
- owns: the strict provider request and response contracts (the authoring output topology and its function contracts); the bounded round loop, its policy knobs (round cap, bookkeeping completion, progress budget, partial acceptance, stall escalation) and its resume seam; decoding a provider's full or edit-shaped answer into a typed GeometryProgramProposal without inventing geometry; semantic coverage validation of a proposal against the state, commitments, seat scope and required component bindings; the lineage / round / proposal / deferral / escalation records and their persistence through the repository; reloading one exact compiled program from its retained record
- does not own: compiling the proposal into objects and digests - compilers.geometry; the request and receipt contracts it invokes a provider with - ports.model; accepting a stage or writing canonical state - it holds no hard-gate or canonical-write authority; producing geometry from records itself - capabilities.element_producers (the runner feeds it an authored proposal); any CAD translation or execution - archflow.adapters
- api: `produce_geometry_program_proposal`, `resume_geometry_program_proposal`, `load_geometry_proposal_lineage`, `load_compiled_geometry_program`, `proposal_authoring_output`, `proposal_edit_authoring_output`, `GeometryProposalPolicy`, `GeometryProposalProviderIdentity`, `GeometryProposalStatus`, `GeometryProposalProductionError`
- invariants: no fallback: a round that does not compile is rejected with issues, never patched into acceptance; the provider receipt's identity must match the declared GeometryProposalProviderIdentity; the proposal is bound to one exact design-state digest and predecessor program digest; a round is capped by policy; exhausting the cap deposits a deferral or escalation record, not a result; every round is retained before the next one is authored

### capabilities.opening_solver — `archflow/capabilities/opening_solver.py`
Fill a wall's hosted voids with window and door types, emitting frame, glazing and leaf members as one hosted assembly per opening.
- owns: WindowType and DoorType: the proportions of one opening family (frame width/depth/projection, glazing or leaf thickness, offsets, leaf count and clearances); solving a void into frame members, a pane, or jambs/head plus one or two leaves; the arrayed form: one member definition placed void.count times along the wall; assembling the members into a HostedAssembly with its roles and interface reference
- does not own: where the opening is, how big it is, or which storey it belongs to - capabilities.wall_solver's HostedVoid says; cutting the host - capabilities.wall_solver owns the tool and the boolean; choosing which type an opening gets - capabilities.element_producers maps opening id to type; any project coordinate or level elevation
- api: `WindowType`, `DoorType`, `OpeningSolution`, `OpeningSolverError`, `solve_window`, `solve_door`, `solve_openings`
- invariants: a type carries proportions only - no position, no level, no absolute size; every member is placed relative to the void the wall granted; a void whose opening id has no type fails OpeningSolverError rather than being left empty

### capabilities.reference_resolver — `archflow/capabilities/reference_resolver.py`
Resolve the record's symbolic plan and elevation references against grids, levels and host lines - the one resolver every producer calls.
- owns: the reference vocabulary: GridRef, GridIntersection, AxisPoint, HostAlong, LevelRef, OffsetFrom; parsing a reference from its JSON form; resolving a plan reference to one (x, z) coordinate; resolving an elevation reference to a symbolic (level datum id, offset) pair; ReferenceContext: the grids, levels and host lines a resolver may read; HostLine: a host element's reference line and points along/across it
- does not own: emitting any geometry operation from a resolved point - capabilities.element_producers; publishing a datum an element grants - capabilities.element_producers; substituting a datum id for its numeric elevation - compilers.geometry; the ProjectLevels / ProjectGrids record shapes themselves - archflow.state.geometry_program; derivation expressions - archflow.state.derivation
- api: `ReferenceError`, `ReferenceContext`, `HostLine`, `GridRef`, `GridIntersection`, `AxisPoint`, `HostAlong`, `LevelRef`, `OffsetFrom`, `parse_reference`, `resolve_plan`, `resolve_elevation`
- invariants: resolve_elevation never returns a number - the compiler is the only thing that turns a datum id into one; a grid role matches exactly; there is no id fallback, and a miss fails ReferenceError naming the role and the roles that exist; an unknown axis, level or host fails ReferenceError rather than defaulting; a resolver reads only grids, levels and host lines; it cannot see components or geometry

### capabilities.relation_checks — `archflow/capabilities/relation_checks.py`
Measure a State Record's relations against realized geometry and record held / violated / unchecked as a RelationCheckReport@1.
- owns: the checker table CHECKERS keyed by check_kind; measuring declared relations; the support-contact measurement: subject top against object bottom plus declared engagement or rise, and against a named datum; the element-standing-on-a-level orientation of the same check; the clearance measurement: the largest per-axis separation between two elements' realized extents against the declared interval_m; the aperture measurement: the opening has finite geometry and lies inside its host in plan and vertically within tolerance; reporting a relation with no validator, or one whose elements have no realized geometry, as `unchecked` rather than as passing; the report's three-state counts, held / fully_checked flags and its digest
- does not own: which relations reach the check - runtime.project_runner passes the record's own declared relations plus the `support` relations the producers built; producing the geometry or the analytic bounds it measures - capabilities.element_producers and compilers.geometry; declaring the relation in the first place - archflow.state.state_record; turning a violation into a stage or run verdict - runtime.project_runner; any repair or healing of geometry that fails a check - nothing on the spine does this; giving a zone extent to measure - runtime.project_runner puts each Space@1 in objects_by_element with its volumes' declared bounds
- api: `CHECKERS`, `Checker`, `check_relations`, `check_support_contact`, `check_clearance_interval`, `check_aperture_exists`, `RelationCheckReport`, `RelationCheck`, `RelationCheckError`
- invariants: nothing is healed: a check only measures and reports; CHECKERS and state.record CHECK_KINDS name the same ids; the module refuses to import otherwise, so a declared check is always measured; `held` alone is not a green light - an unchecked relation does not count against it, so held must be shown with fully_checked; support_contact fails RelationCheckError when its objects have no realized bounds; clearance and aperture report `unchecked` naming the element instead; the report carries the record digest it was measured against

### capabilities.wall_solver — `archflow/capabilities/wall_solver.py`
Solve one wall from its location line, thickness, height and storey datum into a solid, a cut tool per opening, and the hosted voids openings are filled into.
- owns: WallElement geometry: reference line, normal, plan points and plan rectangles in wall coordinates; OpeningRequest: where an opening sits along the wall, its width, sill, head and repeat count/step; enumerating the wall solid, one tool per opening and the single boolean difference that cuts them; granting each opening a HostedVoid - its aperture in wall coordinates plus the object ids it may use; rejecting overlapping openings, openings past the wall ends, and openings that hit a declared exclusion
- does not own: what fills a void - capabilities.opening_solver decides frames, panes and leaves; where the wall is in the project - capabilities.reference_resolver resolves its two plan references; the storey elevation as a number - the wall carries a base level datum id, resolved by compilers.geometry; reading opening requests off the record - capabilities.element_producers
- api: `WallElement`, `OpeningKind`, `OpeningRequest`, `HostedVoid`, `WallSolution`, `WallSolverError`, `solve_wall`
- invariants: the wall is one solid; every opening is a void request the wall hosts, never a separate subtraction chain; an opening's position is expressed in wall coordinates (along, across), never in project coordinates; a contract violation - non-positive size, an opening outside the wall, overlapping voids - fails WallSolverError

## compilers

### compilers.geometry — `archflow/compilers/geometry.py`
The one pure compiler: bind a neutral geometry proposal to an exact state, order its operations, digest its objects, or return an explicit rejection receipt.
- owns: the operation dependency graph, its deterministic order and per-object digests; frame, component and semantic-binding digests that bind a program to one state; resolving DatumBinding references into literal parameters (the only place a datum id becomes a number); asset resolution and the substitution receipt when a requested asset is replaced; assembly role validation and revision preconditions against a prior program; the typed issue vocabulary and the compile receipt whose status is ACCEPTED or a rejection
- does not own: authoring or repairing a proposal - capabilities.geometry_proposal; producing operations from records - capabilities.element_producers; any CAD script, execution or readback - archflow.adapters; persisting the program - the caller writes it through archflow.project.repository; accepting a stage - it holds no acceptance authority
- api: `compile_geometry_program`, `resolve_interface_datums`, `CompiledGeometryProgram`, `CompiledGeometryObject`, `GeometryCompilationResult`, `GeometryCompilationReceipt`, `GeometryCompilationError`, `GeometryCompileStatus`, `GeometryIssue`, `GeometryIssueCode`, `AssetSubstitutionReceipt`
- invariants: pure: it reads inputs and returns a result, and never writes or mutates anything; a structurally invalid input raises; a semantically rejected proposal returns a receipt with issues and no program; every issue is appended, not short-circuited, so one round reports every independent failure; the compiled program is bound to the state digest and, on a revision, to the predecessor program digest; operation order is deterministic for one proposal

## contracts

### contracts.authority — `archflow/contracts/authority.py`
The single source of the all-False authority-flag block that every retained record carries.
- owns: the default authority field names (DEFAULT_AUTHORITY_FIELDS); constructing a key-sorted, all-False authority block (no_authority); rejecting a malformed, duplicated or non-'_authority' field name (AuthorityContractError)
- does not own: deciding which record needs which flags (each writer passes its field list); enforcing that a producer really holds no authority (a static scan in the test suite holds the inline-block count at a baseline); record serialization (each record type)
- api: `no_authority`, `DEFAULT_AUTHORITY_FIELDS`
- invariants: every returned flag is exactly False; field names are lowercase identifiers ending in _authority; output keys are sorted, so canonical serialization is independent of caller order

### contracts.canonical — `archflow/contracts/canonical.py`
Deterministic finite-JSON encoding and the sha-256 primitives every record digest on the spine is built from.
- owns: canonical JSON text with sorted keys, no NaN/Infinity and no cycles (canonical_json); its UTF-8 bytes and lowercase sha-256 (canonical_json_bytes, canonical_digest); the 64-hex digest validator (require_sha256); typed rejection of values that cannot be canonical JSON (CanonicalValueError)
- does not own: excluding a record's own digest field before digesting (project.digests); record file bytes on disk, which add a trailing newline (project.repository); which fields a given record digests (each record type decides)
- api: `canonical_json`, `canonical_digest`, `require_sha256`, `canonical_json_bytes`, `CanonicalValueError`
- invariants: a pure function of the value: no filesystem, no clock, no randomness; ascii=True and ascii=False digest differently, so a record keeps the form it was written in; a non-finite float, a non-string mapping key or a cycle raises rather than being coerced

### contracts.fields — `archflow/contracts/fields.py`
Strict scalar and deterministic-tuple validators shared by the newer typed contracts.
- owns: bounded, trimmed, non-empty text (text); enum-instance and finite-number checks (enum_value, finite_number); exact mapping key-set checks used to detect schema drift (exact_mapping); payload field primitives: mapping, list_of, tuple_of, typed_tuple, string_tuple, unique, number, positive, refs, ids; scheme-qualified logical-ref syntax, rejecting file: and drive-letter paths (logical_ref); sorted-unique bounded tuples of refs and identifiers (deterministic_refs, deterministic_identifiers)
- does not own: identifier syntax itself, which it delegates to project.refs.require_identifier; the logical-ref validator used by the operational-state family (state.operational_state.require_logical_ref is a parallel); digesting or serializing the values it validates (contracts.canonical)
- api: `deterministic_identifiers`, `deterministic_refs`, `enum_value`, `exact_mapping`, `finite_number`, `identifier`, `ids`, `list_of`, `logical_ref`, `mapping`, `number`, `positive`, `refs`, `string_tuple`, `text`, `tuple_of`, `typed_tuple`, `unique`
- invariants: a tuple field must already be sorted and unique; the validator never sorts on the caller's behalf; collections are bounded at 4096 items; validation is total: an unexpected type raises rather than being coerced

## ports

### ports.model — `archflow/ports/model.py`
Own the provider-independent model invocation contracts: request, receipt, status, phase and the AsyncModelProvider Protocol.
- owns: ModelInvocationRequest@1 and ModelInvocationReceipt@2, their serde and their schema refs; ModelPhase and ModelInvocationStatus; the AsyncModelProvider Protocol every provider satisfies; the payload validation that keeps a request and receipt portable and JSON-exact
- does not own: executing anything - the caller that crosses the boundary mints the receipt: capabilities.geometry_proposal on the spine, studio.intent in the Studio; what a payload means - the capability that authored the request defines it; any authority, envelope, signing or provider lifecycle: there is none on the spine, and the P053 control plane that had one is archived
- api: `AsyncModelProvider`, `ModelInvocationRequest`, `ModelInvocationReceipt`, `ModelInvocationStatus`, `ModelPhase`
- invariants: a request is bound to a checkpoint digest and a context digest; nothing is invoked without both; a receipt's output is either absent or matched by its output_sha256; contracts only - no module-level execution, no I/O

## project

### project.containers — `archflow/project/containers.py`
Naming the four container states of ADR-007 over the layout: which work is in progress, which run is shared, what is published, and what the archive holds.
- owns: the four container states and the ISO 19650 suitability code each one carries (ContainerState, StatusCode); one container as a value: its state, code, project, run, ref, author, branch and the line a person reads (Container); surveying the authored work-in-progress slots, today the runner's and tomorrow one per author (work_in_progress); surveying the runs as shared containers and awarding S1 or S4 from what each run retains (shared); naming HEAD as the one published container and its issue (published); naming the canonical snapshots HEAD has left behind, oldest issue first (archived)
- does not own: reading or parsing an authored record or seat pack - project.inputs; only existence and the digest of the bytes are read here; naming any path - project.layout; retaining, digesting or verifying anything - project.repository; this module never writes; writing a stage closure or issuing a run as the published design - the runner and project.issue; the stage a run is in - state.stage_workflow
- api: `Container`, `ContainerError`, `ContainerState`, `StatusCode`, `archived`, `published`, `shared`, `work_in_progress`
- invariants: no persistence, no pointer beside HEAD, no write of any kind; records are read through the repository and are digest-verified; only the run and canonical directories the layout owns are listed by path; a run whose manifest or records cannot be read is reported as a shared container saying so, never skipped silently; S4 needs both halves - a runner receipt with seats complete and a SATISFIED stage closure; nothing writes a closure yet, so every run is S1 today; an absent work-in-progress slot is an empty tuple, not a refusal

### project.digests — `archflow/project/digests.py`
The one semantic digest of canonical project-state content, taken with the state's own declared digest field removed.
- owns: digesting canonical state content excluding state_sha256 (project_state_sha256); verifying a declared state_sha256 against the recomputed digest
- does not own: canonical JSON encoding and the raw sha-256 primitive (contracts.canonical); the byte digest of a written record file, which includes a trailing newline (project.repository)
- api: `project_state_sha256`
- invariants: a declared state_sha256 that disagrees with the content raises; the digest is over content without its own digest field, so it is stable under re-declaration; never writes the filesystem

### project.inputs — `archflow/project/inputs.py`
The one reader of a project's work-in-progress inputs: the designer's authored State Record and seat pack, at the two paths the layout owns (ADR-007).
- owns: reading the work-in-progress inputs at the layout's authored_record and seat_pack paths; the typed refusals for those two files - absent, unreadable, undecodable, not JSON, not a State Record - each carrying the path it wanted (InputsError and its four kinds); how each file is identified on the way out: the record's own content digest, and the sha-256 of the seat pack's bytes
- does not own: naming the two paths (project.layout); parsing SeatPack@1 (tools.run_project, the Studio's adapters/seats.py); retaining anything; nothing read here is indexed, digested or verified by the repository (project.repository); deciding which run uses the record (runtime.project_runner, studio.candidate); the meaning of the record it parsed (state.record)
- api: `AuthoredRecord`, `SeatPackFile`, `load_authored_record`, `load_seat_pack_file`, `InputsError`, `AuthoredRecordMissing`, `AuthoredRecordInvalid`, `SeatPackMissing`, `SeatPackInvalid`
- invariants: never writes: the work-in-progress container is read, and nothing about it is retained until a run uses it; absent and unreadable are different refusals, and each names the file it wanted; the record's content digest is taken from the record, never recomputed here (ADR-003); both files are decoded as strict UTF-8; another encoding is invalid, not absent; the seat pack comes back as the mapping the project authored; no seat is parsed, defaulted or invented

### project.issue — `archflow/project/issue.py`
Issuing a run as the published design (ADR-007 rule 5): the one act that moves a shared run to the published container.
- owns: the conditions of an issue: the run's base is the published version, its stage closed SATISFIED with an exit binding naming that closure, and its seats all executed; the typed refusals an issue can end in (IssueError, StaleBase, NoSatisfiedClosure, RunNotComplete); building the replacement CanonicalProjectState@1 from what the run retained: the authored record as authoritative, the developed state and the closure as derived, the closed stage as the phase; minting the PromotionDecision@1 receipt prepare_transition demands, with the closure as its candidate_ref; IssueReceipt: the issue made, the one it replaced, and the records it stood on
- does not own: moving the published position - project.repository, through prepare_transition and compare_and_swap; writing a stage closure or an exit binding - runtime.project_runner writes both from its own checks; the closure and exit-binding contracts - state.stage_workflow; naming a container or reading its suitability code - project.containers; the command line and what it prints - tools.issue_project
- api: `IssueError`, `IssueReceipt`, `NoSatisfiedClosure`, `RunNotComplete`, `StaleBase`, `issue_run`
- invariants: nothing is written until every refusal has passed, so a refused issue leaves the project exactly as it was; there is no issue without exactly one SATISFIED stage closure and the exit binding derived from that same closure, checked by digest and stage; the published state names only records this run retains and the repository verified; the two content digests are not copied out of them (ADR-003); the decision receipt carries exactly the six keys prepare_transition compares; decided_by and note are returned to the caller, never retained; HEAD, PromotionDecision@1, prepare_transition and compare_and_swap keep their names because retained digests and the format bind them (ADR-004)

### project.layout — `archflow/project/layout.py`
Pure mapping from a project root and identifiers to filesystem paths, with no directory creation and no writes.
- owns: the on-disk names of a project's areas (project.json, HEAD, objects/sha256, events, canonical, runs, exports, input); the run subdirectory names (RunLayout: run.json, records, branches, candidates, reviews, workspaces, recovery); resolving a project-relative path or a record/artifact ref to an absolute path inside the root; the names of the two authored work-in-progress files (input/runner/state-record.json, input/runner/seats.json)
- does not own: creating directories or writing files (project.repository); reference validity (project.refs); locating an existing project root (project.location); reading those two authored files (project.inputs)
- api: `ProjectLayout`, `AUTHORED_RECORD_PATH`, `SEAT_PACK_PATH`
- invariants: never creates a directory and never writes; a resolved path that escapes the project root raises; a ref belonging to another project is refused

### project.location — `archflow/project/location.py`
Read-only resolution of where an existing project repository physically lives, and the single helper that opens it.
- owns: the deterministic authority order local then relocated then workspace (locate_project); relocation anchor validation by file digest (ProjectLocation, ProjectLocationKind); checking that the manifest at a candidate root carries the requested project_id (ProjectLocationError); the one open-by-project-id entry point (open_located_project)
- does not own: creating a project or writing state (project.repository); path names inside a project (project.layout); manifest parsing (project.manifest)
- api: `locate_project`, `open_located_project`, `ProjectLocationError`, `ProjectLocationKind`
- invariants: never creates directories and never writes state; a relocation anchor whose digest disagrees is rejected rather than followed; a root whose project.json names another project is rejected

### project.manifest — `archflow/project/manifest.py`
The immutable project.json identity and format-version record.
- owns: project identity on disk (ProjectManifest.project_id); the ArchFlowProject@1 schema and its exact field set; format_version as a positive integer; typed rejection of a drifted or incomplete manifest (ProjectManifestError)
- does not own: reading or writing project.json (project.repository, project.location); what a format version implies for snapshot shape (project.repository)
- api: `ProjectManifest`, `ProjectManifestError`
- invariants: the manifest carries no mutable state: exactly schema, project_id and format_version; a missing or unknown field is an error, never a default

### project.ports — `archflow/project/ports.py`
Persistence port protocols and the validated destination a producer must be handed before it is allowed to write anything.
- owns: the persistence area vocabulary (PersistenceArea); a validated project- or run-scoped write destination (PersistenceDestination); the fail-closed check that a producer was assigned a destination (require_destination, PersistenceDestinationRequired); the structural protocols a repository satisfies (RecordSink, ArtifactSink, CanonicalRepository, ProjectLoader)
- does not own: any implementation of these ports (project.repository is the only one); the filesystem path an area maps to (project.layout); record content or schema (each record type)
- api: `PersistenceArea`, `PersistenceDestination`, `require_destination`, `RecordSink`, `PersistenceDestinationRequired`
- invariants: a run-scoped area requires run_id and a project-scoped area forbids it; branch_id belongs only to the RUN_BRANCH area; nothing here writes; P035 deliberately supplies no filesystem writer; a producer without a destination stops and raises rather than choosing a location

### project.record_kinds — `archflow/project/record_kinds.py`
The table of retained record kinds: every kind the spine writes or reads as a retained record, with the schema its payload declares and what it is for.
- owns: RECORD_KINDS: the one table of retained record kinds, keyed by kind; RecordKind: one kind as a value - kind, schema literal, persistence area, note, and the pattern a computed kind matches; the name constant a writer or reader imports instead of spelling a kind as a literal; the three computed kinds as patterns and their formatters (geometry_proposal_round, geometry_proposal_completion, stage_geometry_program); membership: is_registered and require_registered, exact or by pattern; the six kinds no spine module writes, each saying so in its note: the ADR-007 stage envelope, exit binding and closure Wave C will write, the research bridge's ledger, and the component template and catalog confrontation the proposal producer still accepts
- does not own: the file name a record is stored under, and reading a kind back out of one - project.refs; writing or reading a record - project.repository, which asks this table only on write; validating a payload; the schema literal here says what the writer sets, it checks nothing; what a record means to the design - the module that writes it
- api: `RECORD_KINDS`, `RecordKind`, `geometry_proposal_completion`, `geometry_proposal_round`, `is_registered`, `require_registered`, `stage_geometry_program`
- invariants: a kind is matched exactly, or by its declared pattern; never as a prefix; a kind is in the table only when a spine module writes it, or reads it as a retained record and names its contract; a retired lane's vocabulary is not; schema is the literal the writer's payload actually carries, or null where the payload declares none; this module imports nothing above archflow.project; it reads no state, capability or repository code

### project.refs — `archflow/project/refs.py`
Defines the four stable identity types (project version, run, branch, record, artifact) and the portable identifier and path validators the whole spine reuses.
- owns: branch equality checks (require_exact_branch, require_same_branch); branch identity as run + branch_id + epoch (BranchRef); content-addressed record and artifact references and their project:// uri (ProjectRecordRef, ProjectArtifactRef); portable identifier syntax (require_identifier); project version identity with its optional canonical state digest (ProjectVersionRef, require_digest); project-relative path syntax, normalization and escape refusal (require_project_relative_path); run identity bound to its base version (RunRef); serialising and parsing the four references (to_dict/from_dict, record_ref_from_uri); the one record file-name rule, <kind>-<64 hex>.json, and reading it back (RECORD_FILE_NAME, record_file_name, parse_record_file_name, ProjectRecordRef.record_kind)
- does not own: serializing a BranchRef to and from a dict (contracts.branch); digesting content (contracts.canonical); mapping a ref onto a filesystem path (project.layout); reading or writing anything a ref names (project.repository); logical (scheme-qualified) refs, which are not project refs (contracts.fields.logical_ref, state.operational_state.require_logical_ref)
- api: `BranchRef`, `ProjectArtifactRef`, `ProjectRecordRef`, `ProjectRecordRef.record_kind`, `ProjectVersionRef`, `RunRef`, `parse_record_file_name`, `record_file_name`, `record_ref_from_uri`, `require_exact_branch`, `require_identifier`, `require_project_relative_path`, `require_same_branch`
- invariants: never touches the filesystem; PurePosixPath only; every ref is frozen, slotted and fully validated at construction; a RunRef's base, and a record or artifact ref, belong to the same project_id as their owner; relative_path is already-normalized portable POSIX text with no '..' segment and no drive/backslash; state_sha256, when present, is lowercase 64-hex; require_digest() refuses a ref that lacks one; one parser: a record uri and a record file name are read back here and nowhere else; a name that is not <kind>-<64 hex>.json is refused, not guessed at

### project.repository — `archflow/project/repository.py`
The single durable filesystem owner of one project document: immutable content-addressed records plus one atomic compare-and-swap HEAD.
- owns: the only write path to a project on disk (initialize, put_json, ingest); byte identity of a written record (sorted-key JSON plus a trailing newline, sha-256); HEAD read and compare-and-swap under a thread lock and an OS advisory file lock (compare_and_swap, StaleProjectHead, ProjectHeadLocked); promotion gating on an accepted exact-base PromotionDecision@1 receipt (prepare_transition, PromotionAuthorityError); run creation and the run record areas (create_run, create_run_batch, load_run); format-version compatibility between CanonicalSnapshot@1 and CanonicalSnapshot@2; integrity verification of the snapshot/event chain and orphan reporting (verify, RecoveryReport)
- does not own: path naming inside a project (project.layout); project identity and format metadata parsing (project.manifest); the semantic digest of canonical state content (project.digests); discovering where a project root physically is (project.location); the meaning of a record; it stores mappings and never validates design content
- api: `FilesystemProjectRepository`, `ProjectRepositoryError`, `ProjectAlreadyExists`, `ProjectIntegrityError`, `StaleProjectHead`, `PromotionAuthorityError`, `ProjectHeadLocked`
- invariants: an existing content path is never overwritten; new content is installed or the write fails; HEAD moves only through compare_and_swap; a mismatch with the expected version raises StaleProjectHead; a stored record whose bytes disagree with its ref sha256 raises ProjectIntegrityError; only an accepted PromotionDecision@1 naming the exact expected version may prepare a canonical transition; a record lands in the area its PersistenceDestination names; events/ and canonical/ areas are re-checked on swap; put_json writes only kinds registered in project.record_kinds; reads (load_json, list_json) are unrestricted so retained runs from the archived lanes stay readable

## relations

### relations.contracts — `archflow/relations/contracts.py`
Persistence-neutral typed contracts for the architectural relation graph, and the relation-kind vocabulary the State Record validates against.
- owns: the node kind vocabulary (ArchitecturalNodeKind, ArchitecturalNode); the core relation vocabulary (ArchitecturalRelationKind) - the one vocabulary the record accepts; the closed participant-role set per relation kind (allowed_relation_roles, RelationParticipant); relation projections and epistemic status (RelationProjection, RelationEpistemicStatus); monotone impact effects and propagation rules (ImpactEffect, RelationPropagationRule); the relation and graph records with their digests (ArchitecturalRelation, ArchitecturalRelationGraph)
- does not own: graph coverage and traversal (deliberately outside this module); checking a relation against materialized geometry (capabilities/relation_checks.py); the record's own Relation shape with its datum role and validator binding (state.record); dependency edges (state.operational_state)
- api: `ArchitecturalRelationKind`, `ArchitecturalNodeKind`, `ArchitecturalNode`, `ArchitecturalRelation`, `ArchitecturalRelationGraph`, `RelationParticipant`, `RelationProjection`, `RelationEpistemicStatus`, `RelationPropagationRule`, `ImpactEffect`, `ArchitecturalRelationContractError`
- invariants: owns no design, stage-acceptance, persistence or canonical-write authority; participant roles are closed per relation kind; an unknown role is refused; the graph is a deterministic projection over exact records, never a mutable store; never writes the filesystem

## runtime

### runtime.project_runner — `archflow/runtime/project_runner.py`
The one runtime loop: run admitted seat rounds from a single State Record, retaining every record the run produces and returning an authority-free stage-run receipt.
- owns: run_project: binding the record to the run, projecting the developed-design view, and the seat round loop; StageExecutionGuard: the retained workflow, envelope, predecessor exit and predecessor closure required before any seat record is written; RunOptions: commitment, the declared live provider identity, branch, coverage strictness, export, workspace and patch-oracle switches; the recorded-proposal provider and its identity; per-seat orchestration: scope, exclusions, production context, proposal authoring, program retention, handover compilation; component coverage: what counts as covered, what is declined, what is undeclared and when that is fatal; assembling what reaches the relation check: the record's own declared relations plus the `support` relations the producers built, each with its validator binding; the export decision: reuse an identical prior export, patch a different one, or rebuild; the retained record kinds (state-record, stage-closure, stage-exit-binding, project-levels, project-grids, selected-spatial-option, discipline-seat, seat-authoring-context, seat-geometry-program, seat-relation-check, seat-handover, seat-round-receipt, runner-run-failure, RunnerRunReceipt@3); the stage closure and exit binding of a run: the CompositeStageClosureReceipt compiled from the run's own relation checks and seat results, and the StageExitBinding derived from a SATISFIED one; the synthetic bounds that make a Space@1 zone measurable: its volumes' declared min/max, entered in objects_by_element under the zone's entity id
- does not own: producing geometry - capabilities.element_producers; compiling or accepting a proposal - capabilities.geometry_proposal and compilers.geometry; measuring a relation - capabilities.relation_checks; seat scope, handover compilation rules and scheduling - capabilities.discipline_seats; CAD translation, execution and inspection - archflow.adapters; persistence and HEAD compare-and-swap - archflow.project.repository; advancing a stage: it opens no stage and holds explicit no_authority for canonical write, design and stage acceptance; building the developed-design projection from a pack - state.record
- api: `run_project`, `RunOptions`, `StageExecutionGuard`, `ProjectRunnerError`, `RecordedProposalProvider`, `RECORDED_PROPOSAL_IDENTITY`, `SeatResult`
- invariants: the record is the only design input; seats arrive beside it and are never part of state; the record is bound to the run and admitted by the stage guard before the first write; a relation the producers built that does not hold in the compiled program aborts the run - nothing is healed; a seat may not publish a datum over a project level or grid axis; with strict_coverage, a seat owning a component with neither an element nor a declination fails; the receipt is authority-free: no canonical write, design or stage-acceptance authority is claimed; a receipt names the provider that produced the proposal, never a declared one; one seat failing to get an accepted proposal ends the round loop rather than continuing; a closure is written for every completed run; an exit binding only for a SATISFIED closure; the closure states one finding per problem: a required check kind nothing measured, a relation of a required kind that did not hold, and a seat that did not finish

## semantics

### semantics.conditions — `archflow/semantics/conditions.py`
The registered spatial conditions a component or connection may form, with meanings and aliases.
- owns: the condition vocabulary (condition.*) and its aliases
- does not own: roles - semantics.roles; relation kinds - relations.contracts
- api: `CONDITIONS`, `CONDITION_IDS`
- invariants: a condition is not an object; one geometry may form several

### semantics.registry — `archflow/semantics/registry.py`
Resolve a semantic string (id, id+id, alias, or a registered compound phrase) to registered ids, or name the nearest ones.
- owns: resolution of semantic text to registered ids (resolve_semantic_kind); nearest-id suggestions for refusals (suggest_semantic); the compound phrases existing records were authored with (COMPOUND_PHRASES)
- does not own: the vocabularies themselves - semantics.roles, semantics.conditions; refusing a record - state.record
- api: `SemanticResolution`, `resolve_semantic_kind`, `suggest_semantic`, `registered_ids`, `COMPOUND_PHRASES`
- invariants: a string that resolves to nothing never enters canonical state; aliases resolve to ids and are never stored

### semantics.roles — `archflow/semantics/roles.py`
The registered roles a component may hold (what it does for the building), with meanings and aliases.
- owns: the role vocabulary (role.*) and its aliases
- does not own: entity schemas - state.record; spatial conditions - semantics.conditions; resolving text to ids - semantics.registry
- api: `SemanticTerm`, `ROLES`, `ROLE_IDS`
- invariants: ids are role.<name>; a term is added with a written reason why existing terms cannot compose it (ADR-006)

## state

### state.commitments — `archflow/state/commitments.py`
Typed normative commitments and their authorized lifecycle transitions.
- owns: the commitment record and its schema (Commitment, CriterionRef); the kind, strength, status and revision-policy vocabularies; which commitments carry hard gate authority (has_hard_gate_authority); the legal status transition table and who may make each transition (transition_commitment, RevisionPolicy); typed authority and transition failures (CommitmentAuthorityError, CommitmentTransitionError)
- does not own: evaluating a satisfaction criterion (validation.engine reads the criterion; it never executes predicates); obligations (state.operational_state.DesignObligation); findings and receipts (validation.model); persisting a commitment (project.repository)
- api: `Commitment`, `CommitmentKind`, `CommitmentStrength`, `CommitmentStatus`, `RevisionPolicy`, `CriterionRef`, `transition_commitment`, `CommitmentAuthorityError`, `CommitmentTransitionError`
- invariants: a transition never mutates history; it returns a new commitment with a predecessor link; an unauthorized actor raises rather than being silently ignored; commitments hold no prompt transcripts, executable predicates, findings or obligations; never writes the filesystem

### state.decision_operator — `archflow/state/decision_operator.py`
Compile one typed design action into a deterministic state delta, a successor operational state and a closure receipt.
- owns: the typed intent record with no write authority (DecisionOperator, StateCondition, ConditionComparator); precondition, lock-authority and commitment-spawn authority checks; the delta and its digest (StateDelta); the explicit deterministic dependency closure over invalidations (ClosureReceipt); refreshing obligation readiness after a delta and minting revalidation obligations; the read-only V1 compatibility record and its migration gate (LegacyDecisionOperatorV1, load_decision_operator_record)
- does not own: the state types it operates on (state.operational_state); commitment lifecycle legality (state.commitments.transition_commitment); phase gates (state.design_maturity); persisting the compiled result (project.repository)
- api: `DecisionOperator`, `StateCondition`, `ConditionComparator`, `compile_decision_operator`, `load_decision_operator_record`, `LegacyDecisionOperatorV1`, `DecisionOperatorMigrationRequired`, `DecisionCompilationError`
- invariants: exact-base: an operator names the state digest it applies to and is refused against any other; a decision carries no write authority; compiling produces values, never a persisted state; invalidation closure is explicit and deterministic, not a blanket invalidation; a legacy V1 operator cannot compile current state

### state.derivation — `archflow/state/derivation.py`
Named quantities as small arithmetic expressions over evidence readings, evaluated in dependency order by a safe parser.
- owns: the DerivationTable@1 and DerivedQuantity records and their digests; the expression grammar and its recursive-descent evaluator (numbers, names, + - * /, unary minus, parentheses, min/max/abs/round/sqrt); dependency-order evaluation and its typed failures: unknown name, cycle, divide by zero, non-finite result (evaluate, DerivationError); reading the names an expression uses without evaluating it (expression_names); '@name' substitution into JSON-like authoring payloads (substitute); the evaluated table with each value's inputs, ready to record as DERIVED facts (EvaluatedDerivations)
- does not own: where readings come from (the caller supplies them); resolving datum, level or grid references (capabilities/reference_resolver.py); recording the results as facts or parameters (state.operational_state, state.record)
- api: `DerivationTable`, `DerivedQuantity`, `EvaluatedDerivations`, `evaluate`, `substitute`, `expression_names`, `DerivationError`
- invariants: never calls Python eval; the operator and function set is closed; an unresolvable name, a cycle or a non-finite result fails typed rather than defaulting; quantity count is bounded; never writes the filesystem

### state.design_portfolio — `archflow/state/design_portfolio.py`
The reloadable ledger of schematic branches, their lineage, and the authorized selection among them.
- owns: the branch, revision and lineage records (DesignBranch, BranchRevision, BranchRevisionRef, LineageKind); the branch lifecycle and its transitions (fork_branch, revise_branch, combine_branches, park_branch, reject_branch, select_branch); who may make or release a selection (SelectionPolicy); resolution of detached expert advice (ExpertAdviceResolution, AdviceDisposition); detached comparison evidence with deliberately no selection authority (ParetoBranchObservation); the append-only transition log and the portfolio digest (PortfolioTransition, DesignOptionPortfolio); the handoff record for the selected branch (SelectedBranchHandoff, compile_selected_branch_handoff)
- does not own: ranking or evaluating options (explicitly not the portfolio's job); hard usability gates (validation/*); promoting canonical state (project.repository); the option content itself (state.spatial)
- api: `DesignOptionPortfolio`, `SelectionPolicy`, `BranchLifecycle`, `BranchRevisionRef`, `ExpertAdviceResolution`, `AdviceDisposition`, `ParetoBranchObservation`, `SelectedBranchHandoff`, `initialize_design_portfolio`, `fork_branch`, `revise_branch`, `combine_branches`, `park_branch`, `reject_branch`, `select_branch`, `attach_pareto_observation`, `compile_selected_branch_handoff`, `DesignPortfolioError`
- invariants: every transition is checked against the expected predecessor portfolio digest; a stale base raises; selection requires a named authority permitted by the selection policy and a decision ref; the portfolio preserves alternatives and their lineage; it never ranks them; never writes the filesystem

### state.developed_design — `archflow/state/developed_design.py`
The design-development state the geometry producer and the discipline seats still read: developed components, attributes, discipline obligations, expert advice and coordination status.
- owns: the developed component and its attributes with their source claims (DevelopedComponent, DevelopedAttribute); the discipline vocabulary and per-discipline obligations (DevelopmentDiscipline, DevelopmentObligation, DevelopmentObligationStatus, DevelopmentObligationPriority); detached expert advice and its resolution by the architect (DetachedDevelopmentAdvice, DevelopmentClaim, DevelopmentClaimResolution, ArchitectDevelopmentDecision); cross-discipline dependencies and their recheck-or-invalidate impact (DevelopmentDependency, DevelopmentDependencyImpact); coordination status, transitions and invalidation receipts (DevelopmentCoordinationStatus, DevelopmentTransition, DevelopmentInvalidationReceipt); the developed-design state digest the geometry compiler binds a program to (DevelopedDesignState.state_digest); the selected schematic carried in from the portfolio (SelectedSchematicInput)
- does not own: candidate promotion or execution authority (explicitly none); the schematic option itself (state.spatial); the design phase vocabulary (state.design_maturity); branch selection and lineage (state.design_portfolio); producing geometry operations from a component (capabilities/element_producers.py)
- api: `DevelopedDesignState`, `SelectedSchematicInput`, `DevelopmentDiscipline`, `DevelopmentObligationStatus`, `DevelopmentCoordinationStatus`
- invariants: no candidate or execution authority; exact-base: every decision and advice names the state digest it was made against; expert advice is detached; only the architect decision changes the state; never writes the filesystem

### state.geometry_program — `archflow/state/geometry_program.py`
The typed, platform-neutral geometry program (operations, frames, assemblies, assets, datums) and the published project levels and grids.
- owns: the generic operation and parameter vocabularies (GeometryOperationKind, GeometryParameterKind, GeometryOperation, GeometryParameter); units, tolerance, affine transforms and coordinate frames (LengthUnit, GeometryTolerance, AffineTransform, CoordinateFrame); semantic bindings from geometry objects back to components, commitments and evidence (SemanticBinding); hosted assemblies and the protocol roles each kind requires (HostedAssembly, AssemblyKind, AssemblyRole, AssemblyMember, required_assembly_roles, DetailMaturity); external asset references and the asset uri pattern (AssetReference, ASSET_URI_PATTERN); revision preconditions and retirements over existing objects (ObjectRevisionPrecondition, ObjectRetirement); published interface datums, parameter-to-datum binding and direction checks (InterfaceDatum, InterfaceDatumKind, DatumBinding, verify_datum_directions); the program proposal record and its digest (GeometryProgramProposal); project levels and grid axes as published datums (ProjectLevel, ProjectLevels, ProjectGridAxis, ProjectGrids, verify_project_datums)
- does not own: producing operations from design content (capabilities/element_producers.py, wall_solver, opening_solver); compiling a proposal into a cad program (compilers/geometry.py); executing or reading back geometry (adapters/cad_execution.py, three_dm_inspector); resolving a datum or grid reference for a producer (capabilities/reference_resolver.py); which levels a project has (state.record.project_levels_of publishes them); canonical json and digests - contracts.canonical
- api: `GeometryProgramProposal`, `GeometryOperation`, `GeometryOperationKind`, `GeometryParameter`, `GeometryParameterKind`, `GeometryTolerance`, `AffineTransform`, `CoordinateFrame`, `SemanticBinding`, `AssetReference`, `ASSET_URI_PATTERN`, `AssemblyKind`, `AssemblyRole`, `AssemblyMember`, `HostedAssembly`, `required_assembly_roles`, `DetailMaturity`, `ObjectRevisionPrecondition`, `ObjectRetirement`, `InterfaceDatum`, `InterfaceDatumKind`, `DatumBinding`, `verify_datum_directions`, `LengthUnit`, `ProjectLevel`, `ProjectLevels`, `ProjectGridAxis`, `ProjectGrids`, `verify_project_datums`, `GeometryProgramError`
- invariants: the operation vocabulary is generic: no project, style or external-tool routing authority; every numeric field is finite and every collection is bounded; a datum id namespace is unique across levels and grid axes (verify_project_datums); a proposal names the design-state digest it was produced from; never writes the filesystem

### state.model — `archflow/state/model.py`
The CanonicalState@1 HEAD shape (facts, commitments, open obligations, artifacts) and the StateRef compatibility alias.
- owns: the canonical HEAD state value and its uniqueness rules (CanonicalState); the plain fact, obligation and artifact-ref value types used by the submission and validation path (Fact, Obligation, ArtifactRef); the compatibility GoalContract used only by walking-skeleton fixtures; creating an initial version-0 canonical state (initialize_canonical_project); the StateRef = ProjectVersionRef compatibility alias
- does not own: design content, which lives in the State Record (state.record); commitment lifecycle (state.commitments); the branch-local operational state (state.operational_state); persisting or promoting state (project.repository)
- api: `CanonicalState`, `StateRef`, `Fact`, `Obligation`, `ArtifactRef`, `GoalContract`, `initialize_canonical_project`
- invariants: facts, commitments, obligations and artifacts each carry unique ids; GoalContract is compatibility/test-only; production hard authority belongs to authorized typed commitments; raw reasoning transcripts, discarded drafts and tool traffic are deliberately absent; a design program ref must belong to the same project; never writes the filesystem

### state.operational_state — `archflow/state/operational_state.py`
The branch-local kernel state (facts, bindings, locks, obligations, dependency edges) that the State Record and every downstream contract reuse.
- owns: portable logical-ref and local-id syntax (require_logical_ref, require_local_id, PORTABLE_LOGICAL_REF_PATTERN); the state domain and fact epistemic vocabularies (StateDomain, FactEpistemicStatus); canonical fact values and facts (FactValue, StateFact); parameter bindings and authority locks (ParameterBinding, StateLock); the design obligation with its status, blockers and activation condition (DesignObligation, ObligationStatus, ObligationCondition); the dependency edge and what propagates along it (DependencyEdge, DependencyEffect); OperationalMarkovState@3 with its sufficient and state digests; the read-only V2 compatibility record and its explicit migration gate (LegacyOperationalMarkovStateV2, load_operational_state_record, OperationalStateMigrationRequired)
- does not own: applying a decision to produce the next state (state.decision_operator); phase gates and deliverables (state.design_maturity); the architectural relation vocabulary (relations.contracts); canonical write authority (explicitly outside the operational state); persisting state (project.repository); commitment lifecycle transitions (state.commitments)
- api: `OperationalMarkovState`, `DesignObligation`, `DependencyEdge`, `DependencyEffect`, `ObligationStatus`, `ObligationCondition`, `StateFact`, `StateDomain`, `FactEpistemicStatus`, `FactValue`, `ParameterBinding`, `StateLock`, `require_logical_ref`, `require_local_id`, `PORTABLE_LOGICAL_REF_PATTERN`, `load_operational_state_record`, `LegacyOperationalMarkovStateV2`, `OperationalStateMigrationRequired`
- invariants: stores only future-relevant compiled consequences: no prompt history, tool transcripts or rejected drafts; blocking edges between obligations must be acyclic; an obligation's status is consistent with its blockers and its exact activation condition; a legacy V2 record has no current-state authority and must be migrated explicitly; never writes the filesystem

### state.program — `archflow/state/program.py`
The compact voxel-era building program (use, footprint, required spaces, clearances) carried by canonical state.
- owns: the BuildingProgram@1 record and its JSON form; footprint targets with tolerance (FootprintTarget); the typed program error codes (ProgramErrorCode, BuildingProgramError); cross-clause feasibility checks (circulation width against footprint, prohibitions against requirements)
- does not own: the design that answers the program (state.spatial); commitments derived from the program (state.commitments); site input (state.site_context)
- api: `BuildingProgram`, `FootprintTarget`
- invariants: dimensions are integers in blocks: the vocabulary is voxel-era, not metric; a program states requirements, not a design; text values must not carry surrounding whitespace; an impossible constraint combination raises rather than being clamped

### state.record — `archflow/state/state_record.py`
The canonical StateRecord@1: a project's design state as typed entities, parameters, relations, obligations and evidence, plus the typed views derived from it. It states no stage: a stage belongs to the run that executes it (ADR-007).
- owns: design content identity, independent of run and base (StateRecord.digest); the binding identity a geometry program cites (StateRecord.state_digest); the entity schema vocabulary (Level@1, GridAxis@1, Type@1, Element@1, Assembly@1, Space@1, Reading@1, Component@1, MassingLevel@1, Volume@1, Connection@1); referential integrity inside one record: unique ids, known parents, known relation endpoints, known parameter inputs, known zone volumes and connection zones; the record's dependency edges and the downstream closure over them (dependency_edges, closure); binding an authored portable record to one run and canonical base (bound_to, run_ref); the record's own relation shape with datum role, propagation and validator binding (Relation, ValidatorBinding); CHECK_KINDS: the check_kind vocabulary a validator may name, one line of meaning each, and the field each kind takes; the component tree, published levels and published grids read out of the record (design_components_of, project_levels_of, project_grids_of); the schematic pack and the bootstrap from a record to the developed-design projection (SchematicPack, schematic_proposal, bootstrap_developed_state, developed_design_view); refusing unregistered semantics on Component@1 and dangling relationship_refs on Connection@1
- does not own: the relation kind vocabulary (relations.contracts.ArchitecturalRelationKind); DesignObligation, DependencyEdge and DependencyEffect (state.operational_state); the DesignComponent and ComponentMaturity shapes (state.spatial); the ProjectLevel/ProjectGridAxis shapes it emits (state.geometry_program); persisting the record (project.repository); compiling geometry from it (compilers/geometry.py via capabilities/element_producers.py); the stage a run opens; the record names none, and from_dict refuses a stage key (runtime/project_runner.py, state.stage_workflow, ADR-007)
- api: `CHECK_KINDS`, `Entity`, `Lineage`, `Parameter`, `Relation`, `SchematicPack`, `StateRecord`, `StateRecordError`, `ValidatorBinding`, `bootstrap_developed_state`, `design_components_of`, `developed_design_view`, `project_grids_of`, `project_levels_of`, `schematic_proposal`
- invariants: never writes the filesystem; digest excludes run_id and base, so the same design content authored in two runs digests identically; a record without a base cannot name its own run; bound_to is the only sanctioned way to attach one; a relation kind outside the kernel vocabulary is refused; a ValidatorBinding names a check the spine can measure: CHECK_KINDS is the whole vocabulary and each id carries its own field shape; geometry programs, validation results, receipts and indexes are derived from the record and never authoritative; developed_design_view is an adapter, not a second source of truth; design_components_of is the single component-tree builder

### state.spatial — `archflow/state/spatial.py`
The schematic option contracts (grid basis, levels, volumes, zones, connections, components) and the stable-id component transition receipt.
- owns: the semantic component, its maturity ladder and its digest (DesignComponent, ComponentMaturity); the massing geometry of an option (SpatialGridBasis, SpatialLevel, MassingVolume, SpatialZone, SpatialConnection); constraint responses to authored constraints (SpatialConstraintResponse, ConstraintResponseStatus); one architect-authored schematic answer and its digest (SpatialOptionProposal); validating a progressive refinement without forking the component tree (compile_component_transition, ComponentTransitionReceipt); the option and option-set records handed to the portfolio (SchematicOption, SchematicOptionSet); site bounds in voxel coordinates (SiteBounds, Coordinate)
- does not own: choosing among options (state.design_portfolio); developing a selected option (state.developed_design); geometry operations and datums (state.geometry_program); observed site input (state.site_context); the design phase a proposal belongs to (state.design_maturity)
- api: `ComponentMaturity`, `ConstraintResponseStatus`, `Coordinate`, `DesignComponent`, `MassingVolume`, `SchematicOption`, `SchematicOptionSet`, `SiteBounds`, `SpatialConnection`, `SpatialConstraintResponse`, `SpatialGridBasis`, `SpatialLevel`, `SpatialOptionProposal`, `SpatialProposalError`, `SpatialZone`, `compile_component_transition`
- invariants: no framework defaults: an option is always project-authored; component ids are stable across refinement; a removed component needs an exact explicit retirement set; a refinement may not cross schematic option identity; never writes the filesystem

### state.stage_workflow — `archflow/state/stage_workflow.py`
The ordered stage sequence of a project and the exact, authority-free envelope and exit binding that guard stage orchestration.
- owns: the stage sequence record, its stages and its digest (ProjectStageWorkflow, ProjectStage); binding one run to exactly one stage with its base, branch and state digest (StageRunEnvelope, open_stage_run_envelope); the SATISFIED exit binding of one independently closed stage (StageExitBinding, StageExitStatus); carrying the exact retained predecessor envelope and exit into a successor (StageRunPredecessor); the fail-closed guards callers use before running or closing a stage (require_stage_run_envelope, require_stage_exit_binding); the design-phase ladder and what each phase is called in RIBA 2020, the AIA phases and the Chinese design stages, with the LOD range it admits (DesignPhase, DESIGN_PHASES, PhaseLadder, PHASE_LADDER, LOD_LEVELS); the stage-exit record the guard reads (CompositeStageClosureReceipt, StageClosureFinding, StageClosureStatus); the two harness workflow ids of ADR-007 rule 4 (HARNESS_WORKFLOW_IDS), read by the Studio's reference-run rule and by the runner's receipt; the rule that a workflow may require only checks the spine can measure (require_measurable), applied where a workflow is frozen and where a stage is opened, never on read
- does not own: running a stage or producing its closure record (runtime/project_runner.py); the design phase vocabulary it references (state.design_maturity); the content of the closure the exit binding points at (capabilities/relation_checks.py via the runner); the authority block shape (contracts.authority)
- api: `CompositeStageClosureReceipt`, `DESIGN_PHASES`, `DesignPhase`, `HARNESS_WORKFLOW_IDS`, `LOD_LEVELS`, `PHASE_LADDER`, `PhaseLadder`, `ProjectStage`, `ProjectStageWorkflow`, `StageClosureError`, `StageClosureFinding`, `StageClosureFindingCode`, `StageClosureStatus`, `StageExitBinding`, `StageExitStatus`, `StageRunEnvelope`, `StageWorkflowError`, `open_stage_run_envelope`, `require_measurable`, `require_stage_exit_binding`, `require_stage_run_envelope`
- invariants: these records are guards only: they never select design, accept a stage, mutate geometry, persist state, promote a candidate or write canonical state; stage 0 has no predecessor; every later stage must name the exact retained predecessor envelope and its SATISFIED exit; SATISFIED is the only exit status the vocabulary admits; record key sets are exact; a drifted payload is refused, never coerced; never writes the filesystem; lod is optional, non-decreasing across stages, and inside the range its phase admits; an absent lod serialises to nothing, so a workflow frozen before the ladder keeps its digest; required_checks name ids in state.record CHECK_KINDS; require_measurable refuses anything else, and from_dict does not, so a retained workflow with a free check id still loads (ADR-004)

## studio

### studio.artifacts — `apps/archflow-studio/api/archflow_studio_api/application/artifacts.py`
List the exported models a run's receipts certify, and serve their bytes under the digest that identifies them.
- owns: reading seat-rhino-execution receipts into ArtifactRecord rows across a project's runs; locating the certified file in the run's workspaces by name and re-verifying its sha256 as it is read; the named unavailability reasons (no digest, file missing, file unreadable, digest mismatch) and which runs a not-found did not search; the content-disposition save-as name in both RFC 6266 forms; the per-path sha256 cache keyed by path, size and mtime
- does not own: producing the export or its receipt - adapters.cad_execution through runtime.project_runner; inspecting the .3dm - adapters.three_dm_inspector; deciding an export succeeded - the retained receipt says so; the project binding and its record refs - studio.binding
- api: `GET /api/artifacts`, `GET /api/artifacts/{sha256}/bytes`, `list_artifacts`, `artifact_bytes`, `ArtifactRecord`, `ArtifactListing`
- invariants: a file on disk is not an artifact - only a retained receipt makes it one; bytes are served only when the file's own hash still matches the receipt's digest; an unavailable artifact is refused in the receipt's own terms, naming the reason and the runs not searched

### studio.binding — `apps/archflow-studio/api/archflow_studio_api/application/binding.py`
Bind the process to one P036 project and one reference run, resolve which project a scoped path names, and project the authored State Record into what a screen can read.
- owns: opening the located project on first use and holding that one binding for the process; resolving the project segment of /api/projects/{project_id}: today only the bound project resolves, and every other id is 404 PROJECT_NOT_FOUND; listing the projects this server binds, and which of them the unscoped default-project shortcut answers for; choosing the reference run: the configured one, or the newest runner receipt whose run actually finished its seats, skipping the harness workflows state.stage_workflow names; reading the P036 record-name convention (kind plus sha256) and listing a run's record refs; answering project.inputs' refusals on the wire: 404 STATE_RECORD_NOT_FOUND and 422 STATE_RECORD_INVALID, each naming the file; the projection: bound view, component tree, Element@1 rows with their numeric fields, parameters, dependency edges; the honesty lines stating what this projection cannot tell you; the component catalog derived from the record and the reference run's inspection: per component its descendant elements, editable capabilities (value, type, source, confidence, status, validator refs), object coverage and closure; per exported object the element it binds to, or MODEL_VISIBLE_CATALOG_MISSING when no Element@1 row answers for it
- does not own: writing, issuing or advancing anything - the binding is read-only; runtime.project_runner is the only writer and project.issue the only issuer; parsing an utterance or making a proposal - studio.intent; running a candidate - studio.candidate; closure and impact - studio.validation, over archflow.state.state_record's one closure rule; HTTP shapes and error bodies - studio.shell; inventing an element or a field for an object that has none - the catalog reports the gap; an authored control has to exist first (studio.intent's declare-control proposal)
- api: `GET /api/project`, `GET /api/projects`, `GET /api/projects/{project_id}`, `GET /api/state`, `ProjectBinding`, `ProjectBinding.open`, `bound_project`, `resolve_project`, `binding_answer`, `project_list_dto`, `record_kind`, `ReferenceRun`, `project_state`, `StateProjection`, `ProjectedElement`, `catalog_of`, `build_catalog`, `Catalog`, `CatalogComponent`, `CatalogElement`, `Capability`, `ObjectBinding`
- invariants: the project root is never defaulted in code - it comes from the environment or --project-dir; binding happens on the first request that needs it, not at import or app creation; every fact on the projection is the kernel's answer: the record is parsed by StateRecord.from_dict and attached by bound_to; a harness run is never chosen as the reference run; the projection is read-only and states, in honesty lines, what it could not compute; one process answers for one project: the scoped and the unscoped path resolve to the same binding and are shaped by the same function; the catalog derives; it never writes: an object no element names stays MODEL_VISIBLE_CATALOG_MISSING, and no neighbouring element's field is offered in its place

### studio.candidate — `apps/archflow-studio/api/archflow_studio_api/application/candidate.py`
Execute a proposal as a detached candidate through the kernel's own runner, and read the finished run back out of the records it retained.
- owns: the successor record: the authored record with exactly the one value the proposal names replaced; arranging the run - new run id, harness stage guard, seat pack, RunOptions - and calling run_project; retaining the intent compilation receipt in the candidate run as intent-compilation; the studio candidate harness workflow and stage, explicitly not a project stage advance; turning the authored seat pack project.inputs read onto SeatSpec, and refusing a pack that declares no seat; the worker pool and queue: closure-based conflicts, the exclusive Rhino export lane, and per-job lifecycle state; the bounded in-process event log, its replay and its live subscribers; CandidateRun: seat outcomes, relation totals as three counts and two flags, artifacts, and honesty lines
- does not own: seat execution, geometry and every record it writes - runtime.project_runner; the validation receipt and the advance verdict - studio.validation; the before/after object comparison served on /api/candidates/{id}/compare - studio.validation's compare; artifact bytes and availability - studio.artifacts; making the proposal - studio.intent; resolving what a request is about, and the pending intent a clarification is carried in - studio.intent; the project binding - studio.binding
- api: `POST /api/proposals/{proposal_id}/candidate`, `GET /api/jobs/{job_id}`, `GET /api/candidates/{candidate_id}`, `GET /api/candidates/{candidate_id}/compare`, `GET /api/events`, `successor_record`, `execute_candidate`, `describe`, `CandidateRun`, `SeatOutcome`, `RelationTotals`, `StaleBaseError`, `JobRegistry`, `Job`, `StudioEvents`, `harness_guard`, `load_seat_pack`, `seats_of`, `seat_from`
- invariants: a candidate is detached: it runs under the studio-candidate harness stage and advances no project stage; the record must not have moved since the proposal was made - a stale base is refused, not merged; run_project calls asyncio.run, so a candidate runs on a worker thread and never on the event loop; two candidates whose closures intersect do not run together, and only one Rhino export runs at a time; relation totals stay three counts plus two flags; they are never collapsed into a single verdict; everything reported about a finished candidate is read back off retained records, not remembered from the run

### studio.intent — `apps/archflow-studio/api/archflow_studio_api/application/intent.py`
Turn an architect's sentence, click and strokes into one grammatical, record-checked proposal - and nothing else reaches the record.
- owns: the intent grammar: four exact utterance forms (set, set to, increase by %, decrease by %) plus one optional keep clause; the IntentProvider seam that resolves an utterance to one numeric target in the record and builds its DecisionOperator; the agent seam that compiles free text into that grammar (deterministic, codex subprocess, Anthropic Messages) and the record sheet an agent may see; the receipt of a Studio model call: the request it was bound to, the provider's version and fingerprint, and how the call ended; resolving a picked object to a component and element from its archflow:* user strings, and saying when it is unbound, unknown or stale; reading circle / arrow / keep / remove gestures into facts the record can vouch for; the target resolver: which component and element a request is about, from the projection's component tree, an explicit pick and what the architect has rejected - never from the nearest similar string; the action resolver: change_existing_value / declare_missing_control / clarify / unsupported, and the AuthoredControlDraft the second of those returns; the four closed outcomes of one intent (COMPILED, NEEDS_CLARIFICATION, MISSING_EDITABLE_CONTROL, UNSUPPORTED) and their reason codes; the PendingIntent value and the in-process store that holds it, keyed by continuationToken and bound to one stateDigest; the Proposal value, its closure, and the in-process store that holds it
- does not own: running the proposal - studio.candidate; what a change reaches and whether it may advance - studio.validation; writing to the record - runtime.project_runner, through studio.candidate; the record and its projection - studio.binding; the request and receipt contracts themselves - ports.model; the HTTP error body and the DTOs - studio.shell
- api: `POST /api/intents`, `POST /api/pick/resolve`, `POST /api/proposals`, `GET /api/proposals/{proposal_id}`, `parse_utterance`, `merge_keep`, `accepted_forms`, `DeterministicIntentProvider`, `compiler_from_settings`, `context_refs`, `record_sheet`, `DeterministicCompiler`, `CodexCompiler`, `AnthropicCompiler`, `IntentAgentFailed`, `resolve`, `read_compilation`, `clarify`, `blocked`, `editable_descendants`, `kinds_in`, `property_in`, `PendingIntent`, `PendingIntentStore`, `CandidateOption`, `AuthoredControlDraft`, `Resolution`, `resolve_pick`, `read_gestures`, `direction_words`, `proposal_from`, `closure_of`, `ProposalStore`, `element_of_object`
- invariants: an utterance is parsed, never interpreted; anything outside the four forms becomes a question with the accepted forms attached; an agent's answer must itself parse as the grammar - a compiler that claims success ungrammatically is a failure; a clarification chain advances or terminates: a round that leaves the target, the missing slots and the candidates where they were is answered UNSUPPORTED, never asked again; a pending intent is bound to a stateDigest; one opened against a state the project has left is void and answers STALE_CLARIFICATION; one intent ends in exactly one of four outcomes, and only COMPILED is a proposal; a rejected candidate is authoritative for the life of the pending intent and is never offered again inside it; a component with no editable descendant answers MISSING_EDITABLE_CONTROL and a draft; it never asks a person for an elementId, a field name or a grammar; a noun names a kind, and a candidate whose kinds do not include it is not a candidate: a request for columns never resolves to a roof, an abutment or a wall; a model call on the Studio produces a ModelInvocationReceipt of the shared contract; the deterministic compiler produces none; a provider that cannot name its own version refuses to be built rather than signing receipts with 'unknown'; a proposal carries the authority id studio:proposal-only; it is proposal-only and never a commit; an AuthoredControlDraft is a value: it is returned, never retained, and it invents no number; identity comes from the export's archflow:* strings, not from an object's name alone; the proposal store and the pending-intent store are one process's dictionaries: not version history, lost on restart

### studio.shell — `apps/archflow-studio/api/archflow_studio_api/main.py`
Assemble the FastAPI application: settings in, routes mounted under /api, one error body out, the named protocol boundary the API serves, and the reserved application ports.
- owns: create_app and the uvicorn entry point, and the lifespan that lets a running candidate finish; StudioSettings and every environment variable the process reads (project dir, rhino export, workers, powershell, reference run, mode, bind host, token, origins); the two modes of the boundary: local is unauthenticated, and a remote process without a token and without allowed origins is refused at construction; the protocol identity the server answers with - archflow/<major>, the server name, the one server version constant - and the capability list computed from settings; the bearer token gate over /api, exempting only health and protocol, and the CORS policy added in remote mode; the one error body {code, detail} plus question and acceptedForms, and - for a refusal belonging to a clarification chain - outcome, pendingIntent and authoredControlDraft, and the handlers that force every failure including a bug into it; the reserved application Protocols (IntentProvider, RetrievalProvider, PreviewBackend, ViewerAssetProvider, HumanReviewPort, StudioEventSink); every wire DTO and its camelCase aliases, and the router mounting order; GET /api/health and GET /api/protocol
- does not own: any design question or kernel call; binding the project - studio.binding does that on first use, never at app creation; resolving which project a project-scoped path names, and listing the projects this server binds - studio.binding; application behaviour behind any route - studio.binding, studio.intent, studio.candidate, studio.validation, studio.artifacts; implementations of the reserved ports: no port may waive validation or write canonical state
- api: `GET /api/health`, `GET /api/protocol`, `create_app`, `main`, `StudioSettings`, `SettingsError`, `StudioError`, `ClarificationRefused`, `BlockedNeedsHuman`, `MissingEditableControl`, `UnsupportedRequest`, `BearerTokenMiddleware`, `error_sentence`, `PROTOCOL`, `PROTOCOL_MAJOR`, `SERVER_NAME`, `SERVER_VERSION`, `server_capabilities`, `IntentProvider`, `RetrievalProvider`, `PreviewBackend`, `ViewerAssetProvider`, `HumanReviewPort`, `StudioEventSink`, `StudioHealth`, `ServerIdentityDto`, `server_identity_dto`
- invariants: create_app touches no filesystem and binds no project - a wrong project root is discovered by the request that needs it; the project root is never defaulted in code; GET /api/protocol opens no project, so a client can tell a foreign server from an unbound one; a remote-mode StudioSettings without a token and without origins cannot be constructed, so no path can start one; in remote mode every /api route but health and protocol answers 401 UNAUTHENTICATED without a matching bearer token, unknown paths included; every failure answers in the same body shape, and an unexpected error says nothing about itself; a refusal that ends a clarification carries a null continuationToken, so a client can tell 'answer this' from 'this is what the system lacks' without reading the sentence; ports.py holds Protocols only: there are deliberately no implementations here; projectBound in health is whether the binding opens, not a claim about what it contains

### studio.validation — `apps/archflow-studio/api/archflow_studio_api/application/validation.py`
Say what a finished candidate validates to - the kernel's receipt - and, separately, whether it may advance and what refuses it.
- owns: building the CandidateSubmission and asking archflow.validation.engine.validate_submission about it; the five-clause advance verdict (receipt, seat execution, relations held, relations fully checked, exports available) and its blocked_by list; the honesty lines about what this receipt effectively proves on a ref-only published state; memoizing one validation per candidate and issue, computed once in this process; the impact arrangement around StateRecord.closure: direct, propagated, protected, conflicts, locks and unknown coverage; the before/after/why comparison of two runs' exported objects, joined by name from retained seat-3dm-inspection records
- does not own: the validators themselves - archflow.validation.engine; the one propagation rule - archflow.state.state_record's closure; running the candidate or reading its receipt - studio.candidate; issuing anything: nothing here writes canonical state or advances a stage - project.issue is the only issuer; reading a .3dm - the comparison joins inspection records, never files
- api: `GET /api/candidates/{candidate_id}/validation`, `validate_candidate`, `validation_key`, `verdict`, `ValidationStore`, `CandidateValidation`, `Verdict`, `impact`, `Impact`, `ImpactLock`, `compare_runs`, `shapes_of`, `Comparison`
- invariants: the receipt and the verdict are deliberately two different answers and are never merged; advance requires all five clauses; every refusal is named in blocked_by; a validation is about one candidate at one issue - the key includes the published version; impact adds no propagation rule of its own; it calls StateRecord.closure and arranges the answer; the comparison is a join on retained records, with a stated tolerance, not browser or geometry inference

## submission

### submission.model — `archflow/submission/model.py`
The candidate submission: the only boundary from speculative work to formal review.
- owns: the submission record and its full content digest (CandidateSubmission.content_digest); the delta a candidate proposes (CandidateDelta: facts, commitments, obligations, artifacts); claims with their evidence refs (Claim); the rule that a candidate may add only PROPOSED commitments
- does not own: running the gates over a submission (validation.engine); the receipt a gate run produces (validation.model); promoting an accepted delta into canonical state (project.repository.prepare_transition)
- api: `CandidateSubmission`, `CandidateDelta`, `Claim`
- invariants: the content digest covers the complete submission, so a receipt proves which delta was checked, not merely which id; claim keys and discharged obligation ids are unique; only PROPOSED commitments may be added by a candidate; never writes the filesystem

## tools

### tools.archcheck — `tools/archcheck.py`
Policy-as-code architecture boundary checks over the source roots, driven by governance/architecture_policy.json.
- owns: loading and validating ArchFlowArchitecturePolicy@1; the checked-file set for the configured roots and the single AST index per file; the checks: forbidden imports, instance-answer literals, filesystem writes outside allowed functions, forbidden framework identifiers, commit soft-gate leaks, probe-boundary executables; the Finding shape and the JSON/text report and exit code
- does not own: the policy content - governance/architecture_policy.json; the work registry and its ledgers - tools.devctl; the module registry rules CANONICAL_SPINE 4 describes (owner-symbol, duplicate-body, archive-import) - not implemented here yet; running tests or type checks
- api: `run_checks`, `load_policy`, `validate_policy`, `PolicyFinding`, `ArchitecturePolicyError`, `main`
- invariants: no archflow import: the checker parses source and never executes it; a policy that cannot be interpreted deterministically fails ArchitecturePolicyError rather than passing vacuously; the file set is stable and de-duplicated, so findings are reproducible

### tools.devctl — `tools/devctl.py`
Read governance/work_registry.json and render the two generated ledgers: the dynamic map and the planning index.
- owns: loading ArchFlowDevelopmentRegistry@2 and its statuses (active, ready, blocked); the readiness rule: an item is ready when no live item blocks it, and an id absent from the registry counts as finished; rendering docs/DYNAMIC_MAP.md, docs/mapping/planning/INDEX.md, docs/SYSTEM_MAP.md and docs/SEMANTIC_REGISTRY.md, including the stage-ladder table generated from PHASE_LADDER
- does not own: architecture boundary enforcement - tools.archcheck; the module registry described in CANONICAL_SPINE 4, which is a separate file from work_registry.json; any archflow behaviour: it reads the semantic tables and the phase ladder to render them and defines none of them
- api: `load_registry`, `is_ready`, `render`, `main`
- invariants: the registry holds only unfinished work; a finished card is deleted along with its entry; both ledgers are generated, never hand-edited; the ladder table is generated from PHASE_LADDER, never hand-written, so the map cannot drift from the enum

### tools.freeze_project_stage_workflow — `tools/freeze_project_stage_workflow.py`
Validate and retain a ProjectStageWorkflow@1 through the P036 repository, and prove that freezing it issued nothing.
- owns: the freeze command line (project root, run id, workflow path, create-run); validating the workflow payload and storing the exact bytes as a record; the freeze receipt recording the published version before and after; refusing a workflow whose required_checks name anything outside the checker table, before the run is created and before anything is written
- does not own: opening stage 0 or accepting a stage - archflow.state.stage_workflow and runtime.project_runner; converting legacy runs into evidence, or creating any geometry; the workflow schema itself - archflow.state.stage_workflow
- api: `freeze_workflow`, `main`
- invariants: the stored payload is exactly what was validated; the published version must be unchanged across the freeze, and the receipt says so; the record is authority-free; a workflow naming a check the spine cannot measure never enters the project (ADR-007 rule 3)

### tools.issue_project — `tools/issue_project.py`
The command that issues one run as the project's published design, and prints the issue it made or the typed reason it refused.
- owns: the issue command line (project root, run id, who decided, an optional note); what an issue prints: the new issue number, the version it replaced, the state digest, and the run, stage, closure and decision refs; exit 1 with the typed reason on any refusal, and no traceback
- does not own: deciding whether a run may be issued - project.issue; moving the published position - project.repository; closing a stage - runtime.project_runner
- api: `build_parser`, `issue_project`, `main`
- invariants: the command checks nothing itself; every refusal is project.issue's, printed with its type; decided_by and note are printed and not retained, because PromotionDecision@1's key set is frozen

### tools.open_stage_run — `tools/open_stage_run.py`
Create a run and retain the StageRunEnvelope@1 that binds it to one stage of the project's frozen workflow (ADR-007 rule 2).
- owns: the open-stage command line (project root, workflow ref, stage index, run id, predecessor run); creating the run against canonical HEAD and computing the state digest its envelope binds, from the work-in-progress record with the three constants RunOptions declares; finding the one retained stage-exit-binding of a predecessor run, and refusing a stage N>0 that names none or names a run holding more than one; refusing to open a project stage against a harness workflow; the close obligation the envelope carries: OPEN, naming the stage the runner's closure will answer
- does not own: running the stage or writing its closure - runtime.project_runner; freezing the workflow - tools.freeze_project_stage_workflow; the envelope and predecessor rules themselves - state.stage_workflow; reading the authored record - project.inputs; accepting or issuing anything: opening a stage grants no authority
- api: `open_stage_run`, `StageRunError`, `main`
- invariants: stage 0 opens with no predecessor; stage N>0 opens only against the retained SATISFIED exit binding of the run that closed stage N-1, and refuses before the run is created; a workflow naming a check the spine cannot measure is refused (require_measurable); the state digest the envelope binds is the one the runner recomputes, so the guard admits the run it opened; nothing is closed, accepted or issued here

### tools.reindex_project (None) — `tools/reindex_project.py`
CLI: re-index a project's authored record against named inspection records and write a component-catalog and a draft state-record successor into a new run through the repository.
- owns: the --source <uri>@<precedence> vocabulary; writing component-catalog and state-record records into --out-run; the Markdown coverage summary
- does not own: the derivation - capabilities.element_reindex; any write outside the named run
- api: `main`, `markdown`

### tools.run_project — `tools/run_project.py`
The spine's entry point: open a project, take its authored State Record and seat pack from project.inputs with the retained workflow and envelope, and hand them to run_project.
- owns: the run-project command line (project root, run id, workflow ref, stage envelope ref, export, workspace, powershell); parsing a project:// record URI into a ProjectRecordRef; assembling the StageExecutionGuard from retained workflow, envelope, predecessor exit and predecessor closure records; reading an authored seat payload into a SeatSpec (_seat, reused by tools/verify_state_record.py); --record-ref: running a state-record record held in a run (a candidate successor) in place of the authored record, which is left untouched; --seats-file: partitioning a run by an explicit seat pack (a compile partition for a candidate) instead of input/runner/seats.json, which is left untouched
- does not own: the run itself and every record it writes - runtime.project_runner; seat semantics - capabilities.discipline_seats; creating or freezing a workflow - tools.freeze_project_stage_workflow; proving equivalence against a reference run - tools.verify_state_record; reading the authored record and seat pack - project.inputs; there is no pack directory to point elsewhere; creating the run and retaining its stage envelope - tools.open_stage_run
- api: `main`, `_seat`
- invariants: every input is a retained record loaded by ref; the tool authors no design content; the stage guard is assembled before the runner is called, so a run cannot acquire a stage by side effect

### tools.verify_state_record — `tools/verify_state_record.py`
Prove a State Record reproduces a reference runner run, recording both checks in a fresh run as state-record-equivalence.
- owns: the equivalence command line (project, reference run, target run, object renames); building a self-contained harness StageExecutionGuard so the equivalence run opens its own stage; comparing the record-driven run's compiled programs and analytic object bounds against the reference run's; the state-record-equivalence receipt this run retains
- does not own: running the project - runtime.project_runner; reading the authored record and seat pack - project.inputs; a seat is parsed with tools.run_project._seat; comparing saved .3dm geometry - adapters.three_dm_inspector and studio.validation's compare do that
- api: `main`
- invariants: the equivalence claim is recorded as a retained record, never printed only; the harness workflow is explicitly not a project stage advance

## validation

### validation.engine — `archflow/validation/engine.py`
The read-only hard gates that turn a canonical state plus a candidate submission into a validation receipt.
- owns: the validator protocol (Validator); the artifact-presence and artifact-evidence gate (ArtifactPresentValidator); the production claim gate compiled only from authorized hard commitments (AuthorizedCommitmentClaimsValidator); the obligation-discharge gate (ObligationDischargeValidator); deterministic receipt id derivation from state, submission and findings; fail-closed aggregation: base mismatch and validator exceptions become ERROR findings (validate_submission)
- does not own: the receipt shape (validation.model); the submission (submission.model); promotion after a pass (project.repository.prepare_transition); domain checks such as relations, cad readback or usability (capabilities/relation_checks.py and the archived lanes)
- api: `validate_submission`, `ArtifactPresentValidator`, `AuthorizedCommitmentClaimsValidator`, `ObligationDischargeValidator`, `RequiredClaimsValidator`
- invariants: read-only: the gates never mutate state and never write anything; a base mismatch between the submission and the checked state is itself an ERROR finding; an exception inside a validator fails closed as a validator.exception finding; RequiredClaimsValidator is compatibility-only and refuses to gate production state

### validation.model — `archflow/validation/model.py`
The spine's validation result vocabulary: a finding, and the receipt that binds a verdict to the exact submission content checked.
- owns: finding severity (Severity); the finding value: code, message, severity, evidence refs (Finding); the receipt binding a verdict to a submission digest and a checked state (ValidationReceipt); the pass/fail consistency rule
- does not own: running validators (validation.engine); the deterministic-check envelope used by relation and stage checks (validation.contracts, the parallel being folded away); the submission it reports on (submission.model)
- api: `Finding`, `ValidationReceipt`, `Severity`
- invariants: passed is true exactly when no ERROR finding exists; submission_digest must be 64-hex: a receipt cannot float free of the content it checked; never writes the filesystem

## Stage ladder

Two axes: the phase (RIBA 2020 / AIA / 中国) and the BIMForum Level of
Development inside it. A `ProjectStage` states an optional `lod` from 100, 200, 300, 350, 400, 500; LOD 500 is field-verified as-built and belongs to no design phase.

| phase | RIBA 2020 | AIA | 中国 | LOD range |
| --- | --- | --- | --- | --- |
| `research_brief` | 0 Strategic Definition | pre-design | 前期调研 / 项目建议书 | — |
| `programming` | 1 Preparation and Briefing | programming | 任务书 / 策划 | — |
| `site_resource_coordination` | 1 Preparation and Briefing (site information) | pre-design | 场地 / 资源条件 | — |
| `schematic_design` | 2 Concept Design | Schematic Design | 方案设计 | 100–200 |
| `design_development` | 3 Spatial Coordination | Design Development | 初步设计(扩初) | 200–300 |
| `candidate_coordination` | 3 Spatial Coordination (coordination of alternatives) | DD coordination | 扩初深化 / 专业配合 | 300–350 |
| `execution_ready` | 4 Technical Design | Construction Documents | 施工图设计 | 350–400 |
