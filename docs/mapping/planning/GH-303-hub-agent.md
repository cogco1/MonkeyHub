# GH-303

Issue: https://github.com/cogco1/MonkeyHub/issues/303
Base: `bf80accc` (batch H2 wave 1, after batch H1).

The Hub Agent reviews visually through one bounded tool: visual_review calls the runtime route under a per-turn budget the Hub holds, returns findings rather than images, and is traced as visual_observation; the Agent can also place drawing dressing through studio_request.

Batch H2 (2026-09-25), in the owner's order; plans are kept outside the repo.
