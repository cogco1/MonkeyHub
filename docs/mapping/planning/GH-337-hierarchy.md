# GH-337

Issue: https://github.com/cogco1/MonkeyHub/issues/337
Base: `9ca2d900`.

One information hierarchy across the Hub (#337). Increment 2: the Design Tree canvas draws in the Hub's own colour tokens, so it follows the theme and all four interface styles instead of Excalidraw's dark-mode inversion. Merging main (#336) joins #322's keyboard work in ChatShell.tsx with the menu row, and brings the chat-shell suite back to running on main.
