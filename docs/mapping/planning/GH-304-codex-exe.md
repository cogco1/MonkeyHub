# GH-304 Codex executable on Windows

Issue: https://github.com/cogco1/MonkeyHub/issues/304

Resolve the configured Codex executable through PATH when Studio settings are built, so the npm shim codex.cmd launches without a shell.
