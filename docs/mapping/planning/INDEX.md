# Planning ledger

> Generated from `governance/work_registry.json`.

| ID | Status | Goal | Card |
| --- | --- | --- | --- |
| P094 | active | Produce the CAADRIA 2027 full paper from the submitted abstract and frozen records: Chinese working draft first, then the English manuscript in the official template; every number resolves to a retained record, every abstract promise is marked delivered / to-record / to-run / to-decide, and the contract and outline are re-based on the submitted abstract. | [P094](P094-caadria-2027-manuscript.md) |
| P104 | ready | A resident Rhino export host: one COM process across the exports of a run, with exact ownership proven per script and document isolation between exports, so the incremental patch path's saved work turns into saved wall clock. Retires one process per export; the per-export bridge stays as the fallback and the equivalence oracle. | [P104](P104-resident-rhino-export-host.md) |
| P105 | ready | Give the canonical producers the vocabulary the monuments are written in: solids of revolution and the boolean chain the Pantheon's dome and drum need, and the Doric and Ionic member families plus the roof and entablature fields the Parthenon's 687 retained operations are made of, dimensioned by a derivation table in module ratios and placed by grid reference. Retires that vocabulary as operation kinds invented inside a per-building tool. | [P105](P105-classical-order-producers.md) |
| P106 | blocked | Extract Pantheon and Parthenon State Records from their retained records, replay them through the runner with object-by-object equivalence receipts, inventory every monolith entry point against where its behaviour lives afterwards, then retire the per-building tools to typed stubs. Retires the per-building runner monoliths, once their behaviour is reachable without them. | [P106](P106-monument-replay-and-monolith-retirement.md) |
| P107 | ready | An in-process OCCT executor interprets the same CompiledGeometryProgram the Rhino path runs, tessellates preview meshes, measures bounds against the analytic predictor, exports STEP always and mesh .3dm via rhino3dm, and drives incremental re-execution from the patch selector. Receipts carry an evidence tier (self-measured cold-read vs independent instrument). Retires Rhino as the only execution path; the Rhino executor becomes the stage-gate certifier. | [P107](P107-occt-in-process-executor.md) |
| P108 | ready | The vibe modeling frontend rebuilt inside the apps/archflow-studio namespace as web/ (Vite+React, OpenAPI-generated client) and api/ (FastAPI + Pydantic + uvicorn as BFF only: request validation, SSE, task lifecycle; state, dependencies, validation, geometry and commit are archflow calls), executed by a separate session under Kaiwen's direction. Retires Preview Slice 01 (stdlib gateway, presence-probe kernel, monolithic shell, old launcher) behind tag studio-preview-slice-01; migrates the viewer assets and the six reserved protocols by moving. Round one stops at the validation receipt with no canonical commit. Bound by the machine-enforced firewall on api/, the DO-NOT-REBUILD inventory, the migrated ports seam, and the AGENTS.md one-in-one-out rule. | [P108](P108-vibe-modeling-frontend.md) |
| P110 | ready | A CanonicalState projection for State-Record projects: canonical_state_from_dict accepts only CanonicalState@1, while a State-Record project's HEAD (load_current_state) is the ref-based CanonicalProjectState@1 and its snapshot record is CanonicalSnapshot@2, so validate_submission can only be given CanonicalState(ref=head) with empty facts, commitments and obligations, on which ObligationDischargeValidator and AuthorizedCommitmentClaimsValidator have nothing to check. Project open_obligations from record.obligations, commitments from the record's authorized commitments, artifacts and facts from the run's receipts; one named path beside developed_design_view, or canonical_state_from_dict learns the ref-based shape, never both. Retires the labelled-vacuous validation receipt in P108. | [P110](P110-canonical-state-projection.md) |
| P111 | ready | One continuing design cycle in MonkeyArch: retain an unresolved question, compare alternatives, carry human rejection and revision judgments into a second round, recover after a service restart, and explicitly accept a checked result as the next project base. Extend existing owners and retire process-only retention for work that must survive sessions. P108 and P110 gate final acceptance; initial gap inspection can proceed. | [P111](P111-continuing-design-cycle.md) |

[Back to RMPA](../README.md)
