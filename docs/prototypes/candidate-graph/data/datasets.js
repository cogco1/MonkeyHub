/*
 * Datasets the dev switcher offers. Each `name` is a file data/<name>.js that
 * assigns window.CANDIDATE_GRAPH_DATA. Open one with #data=<name>; without the
 * parameter the page loads "issue-fixture". Add an entry here when adding a file.
 */
window.CANDIDATE_GRAPH_DATASETS = [
  { name: "issue-fixture", label: "Issue #284 fixture" },
];
