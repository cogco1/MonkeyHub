# state

State: the record and its projections. `StateRecord@1` (content identity), the developed design view (binding identity), geometry programs, the stage workflow and its ladder, the design portfolio, commitments and operational state, the program sheet and shared compiled geometry values. Massing calculations live in `monkeyarch.capabilities.massing_metrics`.

Modules and owners: `docs/SYSTEM_MAP.md` (rendered from `governance/module_registry.json`; one owner per capability). This README says what the package is for; it does not repeat the map.

Current owners: state.commitments, state.decision_operator, state.derivation, state.design_portfolio, state.developed_design, state.geometry_program, state.model, state.operational_state, state.program, state.program_sheet, state.record, state.spatial, state.stage_workflow.
